<%= form_with(model: product_category) do |form| %>
  <% if product_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(product_category.errors.count, "error") %> prohibited this product_category from being saved:</h2>

      <ul>
        <% product_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :category, style: "display: block" %>
    <%= form.select(:category_id, Category.all.collect { |s| [s.id, s.id] }, {:include_blank => true, :selected => form.object.id}) %>
  </div>
  <div>
    <%= form.label :product, style: "display: block" %>
    <%= form.select(:product_id, Product.all.collect { |s| [s.id, s.id] }, {:include_blank => true, :selected => form.object.id}) %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
